export interface IBankSlipComposition {
  field_1: {
    group_a: string;
    group_b: string;
    group_c: string;
    check_digit: string;
  };
  field_2: {
    group_d: string;
    check_digit: string;
  };
  field_3: {
    group_e: string;
    check_digit: string;
  };
  field_4: {
    group_k: string;
  };
  field_5: {
    group_u: string;
    group_v: string;
  };
}
