export interface IBankSlipConcessionaireComposition
 {
  field_1: {
    group: string;
    check_digit: string;
  };
  field_2: {
    group: string;
    check_digit: string;
  };
  field_3: {
    group: string;
    check_digit: string;
  };
  field_4: {
    group: string;
    check_digit: string;
  };
}
